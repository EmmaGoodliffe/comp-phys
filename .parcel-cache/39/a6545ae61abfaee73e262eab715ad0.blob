"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

class Particle {
  constructor(sketch, m = 1) {
    this.m = m;
    const _ = sketch;
    this._ = _;
    this.s = _.createVector();
    this.v = _.createVector();
    this.a = _.createVector();
    this.w = 20 * m;
    this.hue = 192;
  }

  update() {
    const {
      _
    } = this;
    this.v.add(this.a);
    this.s.add(this.v);
    this.a = _.createVector();
  }

  draw() {
    const {
      _,
      s,
      a,
      w,
      hue
    } = this;

    const strokeCol = _.color(`hsl(${hue}, 100%, 50%)`);

    _.stroke(strokeCol);

    _.strokeWeight(2);

    const fillCol = _.color(`hsla(${hue}, 100%, 50%, 0.5)`);

    _.fill(fillCol);

    _.circle(s.x, s.y, w);

    _.strokeWeight(4);

    const arrow = {
      line: a.copy(),
      tips: []
    };
    arrow.line.setMag(1.5 * w);
    arrow.line.add(s);

    const theta = _.tan(arrow.line.y / arrow.line.x);

    const theta1 = theta + 180 + 45;

    const r1 = _.line(s.x, s.y, arrow.line.x, arrow.line.y);
  }

}

exports.default = Particle;